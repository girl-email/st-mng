import React, { FC } from 'react';
import styles from './index.module.less';

const Login: FC = () => {

    return (
        <div className={styles.login_main}></div>
    );
};

export default Login;