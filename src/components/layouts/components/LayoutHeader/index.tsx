import React, { FC } from 'react';
import { Layout } from 'antd';

const LayoutHeader: FC = () => {
    const { Header } = Layout;

    return (
        <Header>
            
        </Header>
    );
};

export default LayoutHeader;